version: '3.8'

services:
 

  # Servicio de la aplicación
  front:
    build:
      context: ./
    container_name: front
    image: sebastianwebapp/front_practica:latest
    ports:
      - "80:80"   
    restart: always    
